package com.example.demo.external.gemini.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GeminiConfig {
    // Gemini API 관련 설정이 필요한 경우 여기에 추가
    // 현재는 application.properties의 설정만으로 충분
}
